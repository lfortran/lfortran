# ASR Helper Nodes

These are the product types and the small sum types of `ASR.asdl`: the pieces
that appear inside the nodes rather than on their own. A product type is
written as a list of members with no constructor name of its own, and is
printed in ASR text as `(name :member value ...)`.

```{toctree}
---
maxdepth: 1
---
alloc_arg
array_index
attribute
call_arg
case_stmt
coarray_index
codimension
dimension
do_loop_head
rank_stmt
reduction_expr
require_instantiation
tbind
type_stmt
```
