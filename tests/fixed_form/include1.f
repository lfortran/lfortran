      include "do1.f"

      program main
c TODO: SymbolTable stays empty upon running with `--show-asr`. Needs
c rechecking
      implicit complex (a,b,c)
      implicit character*4 (k,l)
      end program
