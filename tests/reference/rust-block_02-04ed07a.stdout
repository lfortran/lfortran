use num::complex::Complex;
use num::traits::Pow;
use ndarray::{array, Array1, Array2, Array3, Array4, Array5, Array6};

pub fn square(b: i32) {
    let mut result: i32 = 0;
    result = b * b;
    println!("{}", result);
}

fn main() {
    let mut a: i32 = 0;
    a = 10;
    a = 10;
    {
        let mut b: i32 = 0;
        a = a + 5;
        if a == 15 {
            /* WARNING: GOTO is not supported in Rust! */
        }
        b = a / 2;
        square(b);
    }
}
